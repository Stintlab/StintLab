import { Routes } from '@angular/router';
import { StintplannerComponent } from './stintplanner/stintplanner.component';

export const routes: Routes = [
  {
    path: '',
    component: StintplannerComponent
  }
];
